

const machine = [
    { value: "rock" },
    { value: "paper" },
    { value: "scissors" }
];

const randomIndex = Math.floor(Math.random() * machine.length);
const randomChoice = machine[randomIndex].value;



const userRock = document.createElement("button");
const userPaper = document.createElement("button");
const userScissors = document.createElement("button");


if (user === "piedra" && machine === "papel");
console.log("user wins!");